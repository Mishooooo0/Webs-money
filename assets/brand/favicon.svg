<!--
  THE BASE · ★ RESKIN FILE 3e — the favicon.
  The one brand file rendered as a real image rather than a mask, so it
  carries its own colours. They match the deep and ground tokens in
  tokens.css. Each template branch replaces it.
-->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <rect width="64" height="64" rx="14" fill="#3b3531"/>
  <path d="M24 3 C35 15 35 33 24 45 C13 33 13 15 24 3 Z" fill="#f5f2ed"
        transform="translate(32 32) scale(1.05) translate(-24 -24)"/>
</svg>
